<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IscaeAnnuaire.Views.EtudiantsPage"
             Title="Étudiants"
             BackgroundColor="#F5F5F5">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="+" Clicked="OnAjouterClicked"/>
    </ContentPage.ToolbarItems>

    <Grid RowDefinitions="Auto,Auto,*">

        <Frame Grid.Row="0" BackgroundColor="White" Padding="10" Margin="10,10,10,5" CornerRadius="10">
            <StackLayout Spacing="8">
                <SearchBar x:Name="SearchBar"
                           Placeholder="Rechercher un étudiant..."
                           TextChanged="OnSearchChanged"
                           BackgroundColor="White"/>
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Picker x:Name="PickerDept" Grid.Column="0"
                            Title="Dept." TextColor="Black" TitleColor="Gray"
                            SelectedIndexChanged="OnFiltreChanged">
                        <Picker.Items>
                            <x:String>Tous</x:String>
                            <x:String>MED</x:String>
                            <x:String>MQI</x:String>
                        </Picker.Items>
                    </Picker>

                    <Picker x:Name="PickerNiveau" Grid.Column="1"
                            Title="Niveau" TextColor="Black" TitleColor="Gray"
                            SelectedIndexChanged="OnFiltreChanged">
                        <Picker.Items>
                            <x:String>Tous</x:String>
                            <x:String>L1</x:String>
                            <x:String>L2</x:String>
                            <x:String>L3</x:String>
                            <x:String>M1</x:String>
                            <x:String>M2</x:String>
                        </Picker.Items>
                    </Picker>

                    <Picker x:Name="PickerStatut" Grid.Column="2"
                            Title="Statut" TextColor="Black" TitleColor="Gray"
                            SelectedIndexChanged="OnFiltreChanged">
                        <Picker.Items>
                            <x:String>Tous</x:String>
                            <x:String>Actif</x:String>
                            <x:String>Inactif</x:String>
                        </Picker.Items>
                    </Picker>
                </Grid>
            </StackLayout>
        </Frame>

        <Label Grid.Row="1" x:Name="LblCount" Margin="15,0" FontSize="12" TextColor="Gray"/>

        <ListView Grid.Row="2" x:Name="EtudiantsList"
                  ItemTapped="OnEtudiantTapped"
                  SeparatorVisibility="None"
                  HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Frame Margin="10,5" CornerRadius="12" BackgroundColor="White" Padding="15">
                            <Grid ColumnSpacing="15">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="50"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Frame Grid.Column="0" BackgroundColor="#2ECC71"
                                       CornerRadius="25" HeightRequest="50" WidthRequest="50" Padding="0">
                                    <Label Text="{Binding Prenom, StringFormat='{0:0}'}"
                                           TextColor="White" FontSize="20" FontAttributes="Bold"
                                           HorizontalOptions="Center" VerticalOptions="Center"/>
                                </Frame>

                                <StackLayout Grid.Column="1" VerticalOptions="Center" Spacing="3">
                                    <Label FontSize="15" FontAttributes="Bold" TextColor="#1B2A6B">
                                        <Label.Text>
                                            <MultiBinding StringFormat="{}{0} {1}">
                                                <Binding Path="Prenom"/>
                                                <Binding Path="Nom"/>
                                            </MultiBinding>
                                        </Label.Text>
                                    </Label>
                                    <Label Text="{Binding FiliereNom}" FontSize="12" TextColor="#2ECC71"/>
                                    <StackLayout Orientation="Horizontal" Spacing="8">
                                        <Label Text="{Binding Niveau}" FontSize="11" TextColor="Gray"/>
                                        <Label Text="{Binding Statut}" FontSize="11"
                                               TextColor="{Binding StatutColor}"/>
                                    </StackLayout>
                                </StackLayout>

                                <Label Grid.Column="2" Text="›" FontSize="24"
                                       TextColor="#1B2A6B" VerticalOptions="Center"/>
                            </Grid>
                        </Frame>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</ContentPage>
